a = int(input("Enter a positive integer : "))
c = 0

c += a
print(f"c += a or c = c + a : {c}")

c = 0
c -= a
print(f"c -= a or c = c - a : {c}")

c = 0
c *= a
print(f"c *= a or c = c * a : {c}")

c = 0
c /= a
print(f"c /= a or c = c / a : {c}")

c = 0
c **= a
print(f"c **= a or c = c ** a : {c}")

c = 0
c //= a
print(f"c //= a or c = c // a : {c}")

c = 0
c %= a
print(f"c %= a or c = c % a : {c}")
